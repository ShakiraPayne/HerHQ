/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['aab22fc9gset2gy2.public.blob.vercel-storage.com',
    'res.cloudinary.com'  
  ],
  },
};

export default nextConfig;